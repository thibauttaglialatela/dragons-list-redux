const initialState = {
couple: [],

}

const duoDragonKnightReducer = (state = initialState, action) => {
    switch (action.type) {

        default:
            return state;
    }
}

export default duoDragonKnightReducer;
